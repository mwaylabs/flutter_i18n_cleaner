name: i18n_cleaner
description: A sample command-line application.
# version: 1.0.0
# homepage: https://www.example.com
# author: Paweł Szot <pszot@pgs-soft.com>

environment:
  sdk: '>=2.5.0 <3.0.0'

dependencies:
 path: ^1.6.0
 csv: ^4.0.3
 yaml: ^2.2.0

dev_dependencies:
  pedantic: ^1.8.0
  test: ^1.6.0
  lint: ^0.2.3

# to use, execute in terminal:
# pub global activate --source path .
# i18n_cleaner
executables:
  i18n_cleaner: